apiVersion: v1
kind: Service
metadata:
  name: fablink-backend-service
  labels:
    app: fablink-backend
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: fablink-backend
