# 운영환경용 패키지
-r base.txt

# 데이터베이스 연결
dj-database-url==2.1.0

# 캐시
django-redis==5.4.0

# 파일 저장소 (AWS S3)
django-storages[boto3]==1.14.2
boto3==1.34.0

# 웹서버
gunicorn==21.2.0
whitenoise==6.6.0  # 정적 파일 서빙

# 모니터링 및 로깅
sentry-sdk[django]==1.38.0

# 보안
django-ratelimit==4.1.0  # API 요청 제한
django-cors-headers==4.3.1  # CORS (이미 base.txt에 있지만 명시)

# 성능 최적화
django-cachalot==2.6.1  # ORM 쿼리 캐싱
django-compressor==4.4  # CSS/JS 압축
